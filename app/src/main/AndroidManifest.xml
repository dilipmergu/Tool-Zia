<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.toolsharing">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ForgotPassword"></activity>
        <activity android:name=".activities.ToolReviewActivity" />
        <activity android:name=".activities.HistoryToolDetailsActivity" />
        <activity android:name=".activities.GetDamagedToolsActivity" />
        <activity android:name=".activities.BookedToolsActivity" />
        <activity android:name=".activities.MyBookedToolsActivity" />
        <activity android:name=".activities.ToolsDamageActivity" />
        <activity android:name=".activities.GetToolReviewsActivity" />
        <activity android:name=".activities.MyFavouriteActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.RegistrationActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MyToolsActivity" />
        <activity android:name=".activities.MyProfileActivity" />
        <activity android:name=".activities.ToolDetailsActivity" />
        <activity android:name=".activities.MyToolDetailsActivity" />
        <activity android:name=".activities.EditToolActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>